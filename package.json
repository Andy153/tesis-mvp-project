{
  "name": "traza-mvp",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "serve public -l 3000 --no-clipboard",
    "start": "serve public -l 3000 --no-clipboard",
    "build": "echo 'Static site — nothing to build'"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
